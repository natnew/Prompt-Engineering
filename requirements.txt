streamlit==1.40.0        # Replace with the latest stable version if different
openai==0.27.8           # Replace with the latest stable version if different
pillow==11.0.0           # Specify a stable version for pillow
pydub==0.25.1            # For audio processing
numpy==1.26.4            # For numerical computations
anthropic==0.2.9         # For Anthropic's Claude models
cohere==4.8.0            # For Cohere's Command model
transformers==4.35.0     # For Meta's Llama models (via Hugging Face Transformers)
torch==2.0.1             # For Llama or open-source models (e.g., Mistral) that use PyTorch
azure-cognitiveservices==7.0.0  # For Microsoft models via Azure
baidu-aip==2.2.18.0      # For Baidu's ERNIE API
requests==2.31.0         # For API calls where a specific SDK is unavailable



