--upgrade pip
streamlit==1.25.0  # Replace with the latest stable version if different
openai==0.27.8     # Replace with the latest stable version if different
pillow==9.2.0      # Specify a stable version for pillow


